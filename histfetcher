#!/bin/zsh

# f=( log_${~1}/(user_|)${~2}_*(Non[-1]) )
f=( log_${1}/${2}_*(Non[-2,$]) )
if [[ -n $f ]]; then
    f=$f[1]
    print "Last conversation on "${f[-6,-5]}.${f[-8,-7]}.${f[-12,-9]}":"
    tail $f 
    print -- '---'
else
    echo 'No history found.'
fi
